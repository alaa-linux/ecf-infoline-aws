apiVersion: v1
kind: Service
metadata:
  name: springboot-api-service
spec:
  selector:
    app: springboot-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
